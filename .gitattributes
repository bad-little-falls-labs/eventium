# Enforce LF line endings for all text files to match .editorconfig
# This prevents issues with mixed line endings across different OSes.
* text=auto eol=lf

# Explicitly set common project files to use LF, which is redundant with
# the rule above but serves as clear documentation.
*.cs      text eol=lf
*.sln     text eol=lf
*.props   text eol=lf
*.targets text eol=lf
*.csproj  text eol=lf
*.xml     text eol=lf
*.json    text eol=lf
*.md      text eol=lf

# Designate files that should be treated as binary and not have their
# line endings normalized. Add any other binary formats as needed.
*.png     binary
*.jpg     binary
*.gif     binary